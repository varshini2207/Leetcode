class Solution {
    public int fun(int n)
    {
        int arr[]=new int[256];
        int r=0,i=0;
        int count=0;
        while(n>0)
        {
            if (n % 2 == 1) {
            count++;
        }
        n = n / 2;
        

        }
 
 /*int count=0;
        for(int j=0;j<arr.length;j++)
        {
            if(arr[j]==1)
            {
                count++;
            }
        }*/
        return count;
    }
    public int[] countBits(int n) {
        int a=1;
        int i=0;
        int arr[]=new int[n+1];
        while(i<=n)
        {
          a = fun(i);
          arr[i]=a;
          i++;
          
        }
       return arr; 
    }

}
